# Supabase project credentials (Project Settings → API)
# The anon/public key is safe to expose in a frontend app as long as
# Row Level Security (RLS) policies are configured on every table.
VITE_SUPABASE_KEY=your-supabase-anon-key
